/**
 * @summary GamesData
 * @component
 *
 * @param {object} props
 *
 */
function GamesData({ exampleInput }) {
  return {
    exampleOutput: `Output from GamesData (${exampleInput})`,
  }
}

export default GamesData
