/**
 * @summary StoryData
 * @component
 *
 * @param {object} props
 *
 */
function StoryData({ exampleInput }) {
  return {
    exampleOutput: `Output from StoryData (${exampleInput})`,
  }
}

export default StoryData
