/**
 * @summary ListsData
 * @component
 *
 * @param {object} props
 *
 */
function ListsData({ exampleInput }) {
  return {
    exampleOutput: `Output from ListsData (${exampleInput})`,
  }
}

export default ListsData
