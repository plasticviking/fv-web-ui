/**
 * @summary SongsData
 * @component
 *
 * @param {object} props
 *
 */
function SongsData({ exampleInput }) {
  return {
    exampleOutput: `Output from SongsData (${exampleInput})`,
  }
}

export default SongsData
