/**
 * @summary WordsData
 * @component
 *
 * @param {object} props
 *
 */
function WordsData({ exampleInput }) {
  return {
    exampleOutput: `Output from WordsData (${exampleInput})`,
  }
}

export default WordsData
