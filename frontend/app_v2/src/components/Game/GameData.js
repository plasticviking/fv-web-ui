/**
 * @summary GameData
 * @component
 *
 * @param {object} props
 *
 */
function GameData({ exampleInput }) {
  return {
    exampleOutput: `Output from GameData (${exampleInput})`,
  }
}

export default GameData
