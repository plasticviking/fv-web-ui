import { makeStyles } from '@material-ui/core/styles'

export const VisibilityMinimalStyles = makeStyles({
  base: {
    float: 'right',
    paddingBottom: '10px',
  },
  label: {
    float: 'right',
  },
  dialogDescription: {
    textAlign: 'center',
  },
  dialogContent: {
    textAlign: 'center',
  },
})
