import { makeStyles } from '@material-ui/core/styles'

export const VisibilitySelectStyles = makeStyles(() => ({
  selectBase: {
    float: 'left',
  },
  selectIcon: {
    marginRight: '16px',
  },
  selectLabel: {
    float: 'left',
    padding: '20px',
  },
}))
