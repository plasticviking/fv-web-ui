import { makeStyles } from '@material-ui/core/styles'

export const VisibilitySelectStyles = makeStyles(() => ({
  selectBase: {
    float: 'left',
    paddingBottom: '10px',
  },
  selectIcon: {
    marginRight: '16px',
  },
  selectLabel: {
    float: 'left',
    padding: '10px',
  },
}))
