// NOTE: This `Table` component is really just the presentation layer
// NOTE: `Table/TablePresentation` is reused by other Table* components
import TablePresentation from './TablePresentation'

export default {
  Presentation: TablePresentation,
}
